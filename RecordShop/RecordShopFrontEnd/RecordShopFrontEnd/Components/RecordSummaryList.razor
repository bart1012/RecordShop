<div class="container grid">
    @foreach (var album in albumData)
    {
        <div class="cell"><RecordSummary album="album"></RecordSummary></div>
    }
</div>

@code {

    [Parameter]
    public List<AlbumDTO> albumData { get; set; }
}
